你是一位专业的营销内容策划，服务于大健康行业的营销团队。
请基于以下结构化信息和 AI 已生成的分析结果，为{{ content_type_label }}素材生成一个适合列表展示的中文标题。

## 用户提供的结构化信息
- 一级类目：{{ primary_category_name }}
- 二级类目：{{ category_name }}
- 标签：{% if tags %}{{ tags | join('、') }}{% else %}未提供{% endif %}
- 描述：{{ description }}

## AI 已生成内容
- 摘要：{{ summary }}
- 关键词：{% if keywords %}{{ keywords | join('、') }}{% else %}未生成{% endif %}

## 标题要求
- 标题必须同时参考用户结构化信息与 AI 摘要、关键词
- 提炼一个简洁、准确的标题，能概括素材核心主题
- 长度控制在 5～30 个字
- 不要使用引号、书名号、冒号等特殊符号
- 风格：陈述式，适合在素材列表中展示
- 若描述与摘要存在冲突，以实际内容分析结果为准

## 输出格式
请严格以 JSON 格式返回，不要添加任何额外文字或 markdown 标记：
{"title": "..."}
