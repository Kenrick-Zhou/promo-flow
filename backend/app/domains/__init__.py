# Domain Layer
#
# Pure business domain types with no external dependencies.
# Contains: enumerations, value objects, command objects, output objects.
# Uses only: dataclasses, enum, typing (standard library).
